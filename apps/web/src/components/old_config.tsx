import { createLibrary, createLibraryConfig } from "@storyflow/react";

/*
import { Content, ContentType } from "./Content";
import { Element } from "./Element";
import { Link } from "./Link";
import { Text } from "./Text";
import { Nav } from "./Nav/Nav";
import { NavType } from "./Nav/NavType";
import { NavItem } from "./NavItem/NavItem";
import { NavItemType } from "./NavItem/NavItemType";

export const config = createLibraryConfig({
  name: "sf",
  label: "Storyflow",
  components: {
    Content: ContentType,
    Nav: NavType,
    NavItem: NavItemType,
  },
} as const);

export const library = createLibrary<typeof config>({
  name: "sf",
  components: {
    Content,
    Nav,
    NavItem,
  },
});
*/
